import React from 'react';

const MediaModeration = () => {
  return (
    <div className="media-moderation">
      <h1>Media Moderation</h1>
      {/* Approve/reject images, filter, preview, delete, etc. */}
    </div>
  );
};

export default MediaModeration;
